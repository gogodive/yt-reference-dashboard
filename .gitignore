.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# 생성물
site/

# 수집 원본 — 모니터링 채널 목록이 드러나므로 평문으로 커밋하지 않는다.
# 암호화된 data/state.enc 하나만 커밋한다.
data/*.json

# Layer 2 영상 분석 작업 폴더 (영상·프레임 — 절대 커밋하지 않는다)
work/
